apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-expense-pvc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: postgres-expense
spec:
  replicas: 1
  selector:
    matchLabels:
      app: postgres-expense
  template:
    metadata:
      labels:
        app: postgres-expense
    spec:
      containers:
        - name: postgres
          image: postgres:15-alpine
          env:
            - name: POSTGRES_USER
              value: "postgres"
            - name: POSTGRES_PASSWORD
              value: "postgres"
            - name: POSTGRES_DB
              value: "expense_manager_expenses"
          ports:
            - containerPort: 5432
          volumeMounts:
            - mountPath: /var/lib/postgresql/data
              name: postgres-expense-storage
      volumes:
        - name: postgres-expense-storage
          persistentVolumeClaim:
            claimName: postgres-expense-pvc
---
apiVersion: v1
kind: Service
metadata:
  name: postgres-expense
spec:
  ports:
    - port: 5432
  selector:
    app: postgres-expense
